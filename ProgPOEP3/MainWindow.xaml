<Window x:Class="ProgPOEP3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="400" Width="400">
    <Grid Margin="10">
        <StackPanel VerticalAlignment="Center" >
            <TextBox x:Name="AsciiTextBox"
                     FontFamily="Consolas"
                     FontSize="14"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     IsReadOnly="True"
                     Height="150"
                     Margin="0 0 0 15"/>

            <TextBlock Text="Login" FontSize="18" FontWeight="Bold" Margin="0 0 0 10"/>
            <TextBox x:Name="UsernameInput" Width="200" Margin="0 0 0 5"/>
            <TextBlock Text="Enter username" FontSize="12" Foreground="Gray" Margin="0 0 0 10"/>
            <Button Content="Login" Width="100" Click="Login_Click" Margin="0 0 0 5"/>
            <Button Content="Sign Up" Width="100" Click="OpenSignUp_Click"/>
            <TextBlock x:Name="StatusText" Margin="0 10 0 0"/>
        </StackPanel>
    </Grid>
</Window>
